(function () {
    'use strict';
    // Dummy files to prevent 404s since these modules aren't fully fleshed out for the UI yet
    window.GlucoConnect = window.GlucoConnect || {};
    window.GlucoConnect.rules = window.GlucoConnect.rules || {};
    window.GlucoConnect.rules.food = {};
})();
